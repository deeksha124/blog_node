import { IRouter as IExpressRouter, NextFunction } from 'express';
import { GroupArgs, IResource, IRouter, ResourceType } from './types';
declare class RouteGroup {
    private head;
    private router;
    private middlewares;
    constructor(path?: string, router?: IExpressRouter, middlewares?: NextFunction[]);
    get path(): string;
    getRouter(): IExpressRouter;
    listRoutes(): {
        method: string;
        path: string;
    }[];
    group(...rawArgs: GroupArgs): IRouter;
    resource(arg: IResource | ResourceType): this;
    private callRouter;
    private createProxy;
    private parseGroupArgs;
}
export default RouteGroup;
